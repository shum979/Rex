core {
  spark {
    master.url = "local[*]"
    executor.instances = 2
    executor.memory = 4g
    executor.cores = 2
  }
}
tempdatalocation {
  duplicate = "target/dummyfolder"
}
recon {
  tablelocation = "/user/hadoop/reconresult/record_level"
}

